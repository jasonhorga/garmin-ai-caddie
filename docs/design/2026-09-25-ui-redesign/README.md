# 2026-09-25 打球中界面重设计（手机 + 手表）与自动记杆算法

状态：设计定稿，**尚未实现，没有改 App 代码**。本目录是与用户逐条确认后的原型和决策记录，
供后续实现时对照。三个 HTML 可直接用浏览器打开（同目录的 `hole1-topo.png` 是黑骑士 31795
第 1 洞的真实 topo 渲染，已去掉旧的白圈和旗）。

| 文件 | 内容 | 在线版本 |
|---|---|---|
| `live-play.html` | 手机打球主屏：全屏 topo、两侧按钮、障碍、目标点、旗位 | https://claude.ai/artifact/9nVZucP1Q2qBth53guvP89 |
| `score.html` | 手机记分三屏：本洞记分、计分卡、本场汇总 | https://claude.ai/artifact/59ub5pELG2CnxjKSDxZttN |
| `watch.html` | 手表四屏 + 自动记杆检测 / 推算规则 + 一洞模拟 + 业界对照 + 纠错回流 | https://claude.ai/artifact/71pBy9RmYCWMoeYcMKvg69 |
| `review.html` | 复盘：单场复盘、每洞落点、同屏改杆 | https://claude.ai/artifact/2QzYcBW6s3Divy3mJEp2c3 |
| `pre-round.html` | 开球前：首页、开始一场、备战 | https://claude.ai/artifact/2AxcCJTJjgB4BsebnLWw76 |
| `stats.html` | 成绩、表现分析、球包距离阶梯、时间与频率、成绩分布、球场 | https://claude.ai/artifact/DYpNxxhihWmkWpchTkfe8L |

总原则（用户原话归纳）：更原生的 iOS / watchOS、图形化而不是控件化、简洁；**屏幕上不写提示**；
能自己闭环的逻辑不打扰用户（例如同步状态不显示）。

## 1. 手机打球主屏（`live-play.html`）

- 保留现在的 topo 图，不换图；全屏，不要底部面板；地图可缩放、平移。
- 按钮放两侧：左上返回 + 洞号 / Par / 码数 + 本场成绩；右上三距离（后 / 中 / 前，玻璃卡）；
  左侧竖排 障碍 / 打法（1/2）/ 回到（平移后才出现）；左下 记分；右下 记一杆（白色实心）。
- 障碍显示方式与现在一致：一次一个，红色描边、红点、“前 N / 后 N”黑色标签，底部条带名称和 ‹ ›。
- 果岭：不画圈，只有**一面旗**（旗位或果岭中心）。点果岭进入现有“调整旗位”界面。
- 目标点：单击地图设；按住目标点拖动出现放大镜（100pt、2.35 倍、十字准星，与现有 Touch Target 一致）。
- 旗位界面：
  - 不画虚线轮廓；果岭 1–4 倍缩放，放大后可平移，有“−”和“还原”；点果岭任意处旗子移过去；
    拖出果岭贴最近的边。
  - 显示旗到**前沿 / 后沿 / 左边 / 右边**的码数：图上四条细线 + 端点短横 + 数字；卡片里四格同样数字。
    前后沿当前位置→果岭中心方向，左右按面向果岭。
  - 拖旗子**不弹放大镜**（会挡住上方数字）；旗子与手指保持按下时的相对位置，不被手指挡住。
  - 实现需要果岭边界多边形（App 已有，用于贴边）。

## 2. 手机记分三屏（`score.html`）

- **本洞记分**（打完一洞弹出，也用于计分卡改历史洞）：一屏完成，不再是“推荐 → 手动确认四步”。
  - 总杆：一排可左右滑的记分符号（○ 小鸟、□ 柏忌 …），打开时从 1 杆开始排，建议值已选中，
    点哪里停哪里，不自动居中。
  - 推杆分段按钮；开球三块小球道图（Par 3 隐藏）；罚杆小 −/+。
  - **不写任何提示**（不显示“手表测到几次”“GPS 建议”等来源说明），只预选好。
  - 预选来源优先级：手表挥杆检测 → 手机“记一杆” → 默认标准杆（Par−2 上果岭 + 2 推）。
  - 开球结果由第二杆 GPS 位置对照球道轮廓预选（服务端有球道轮廓，需确认是否下发给 App，
    或由服务端算好再下发）。
- **计分卡**：大号总成绩 + 每洞累计走势线 + 前九 / 后九记分符号卡（OUT / IN）；
  选中某洞后“去这洞 / 改成绩”；“结束本场…”放在这里（新主屏没有菜单）。
- **本场汇总**：与赛后复盘同一版式（成绩 → 记分卡（点成绩看落点）→ 球道命中 / GIR / 推杆 / 罚杆四格），
  保存后直接成为复盘页。**不显示同步状态**：保存先落本地，后台上传，没网排队自动补传。
  “放弃本场”先确认并写明会删除什么。
- GIR 不单独记录，由总杆和推杆推算，汇总里标“按推杆推算”。
- 默认值保存会让统计虚高：每洞记下成绩来源（手表 / 手机 / 默认），统计推杆和 GIR 时跳过未改过的“默认”洞。

## 3. 手表（`watch.html`，以后记分主力在手表）

- **本洞**：上下滑三页（右侧小点），和手机同一 topo、同一三距离。
  1. 方案：地图画 AI 球童路线和落点（“D 224”“3W 205”）；左侧绿色球杆签 = 下一杆用杆，**点它换方案**。
  2. 障碍：一次一个，红框 + 前 / 后；左侧大数字 = 到前沿，**点“1 / 4”换下一个**，只列前方的。
  3. 果岭。
  - 三页都是：**表冠只管缩放**（1–4 倍），放大后拖动平移；未放大时上下滑翻页。
  - 测距：未放大时点地图放黄圈；**放大后长按 0.5 秒**放黄圈（避免拖动误放）；显示到黄圈码数和“再 N”到果岭；
    点黄圈取消。**还没开球时一律从发球台量起**（测距、三距离、方案路线），蓝点仍画真实位置。
  - 去掉“手动记杆 +”和每杆“刚才用哪支杆？”：测到挥杆震一下，底部闪“第 2 杆 · 一号木 231 码”，点它撤销。
- **本洞成绩**（走下果岭朝下一洞发球台走，或下一洞开球时自动弹出；不点确认则下一洞开球时自动保存）：
  - 总杆：左右滚动的数字带，中间最大，两侧相邻数字缩小变淡，拖动 / 表冠滑到中间变大；上下拖也可。
  - 推 / 罚：原地上下滚（不弹单独页面），一圈循环（推 0–5，罚 0–4，0 上面接最大值），停手即选定并收起。
  - 总杆自动不低于“推杆 + 罚杆 + 1”，不够时自动加（带动画）。
- **计分卡**：一屏 18 洞记分符号，点某洞改成绩，结束本场在此。
- **本场汇总**：球道 / GIR / 推杆 / 罚杆，保存并结束；“继续打球 / 放弃本场”需往下翻才看到。

## 4. 自动记杆：检测与推算

罚杆（下水、OB、不可打）**不猜**，由用户自己记。检测只负责把每一次动作分对类。

**动作分类特征**（前四项手表实时算，最后一项靠之后的 GPS）：

| 动作 | 击球前站定 | 转动 | 撞击 | 之后 | 处理 |
|---|---|---|---|---|---|
| 真击球 | ≥ 1 秒几乎不动 | 峰值 ≥ 3 rad/s，累计转角完整 | 尖锐，通常 > 3 g，< 12 ms | 离开原地 | 算一杆 |
| 试挥打地 | < 0.6 秒 | 与真挥杆相近 | 发闷，约 2–3 g，20–40 ms | 原地不走，很快再挥 | 同位置 60 秒内只留“最像击球”的一次 |
| 空挥 | 很短 | 完整或半截 | 无（< 1 g） | 原地 | 不算 |
| 打完原地比划 | 无 | 多半截 | 无 | 原地 | 不算，不顶掉刚才那一杆 |
| 推杆 / 试推 | 推杆 ≥ 1 秒 | 慢摆 0.3–2 rad/s | 推杆 0.25–1.2 g 小尖峰；试推无 | — | 只在果岭上判 |
| 坐车等 | — | 无挥杆式转动 | 可能很大 | 车速 > 2.5 m/s | 不算 |

“最像击球”打分：站定越久、撞击越尖越短、越大、之后越快离开，分越高；同分取最后一次。

**流程**：传感器（800 Hz 加速度、200 Hz 转速，需 Series 8+、锻炼模式）→ 挥杆分类 → 定位置（GPS
对照发球台 / 球道 / 长草 / 沙坑 / 果岭，精度 ≤ 12 m）→ 合并去重（手表 + 手机，相距 < 8 m 算同一位置）
→ 分洞（第 N 洞发球台开球进入；离开果岭 25 m 以上朝下一洞发球台走 = 结束）→ 推算成绩。

**推算**：总杆 = 算杆的挥杆 + 推杆 + 罚杆；推杆没测到时按第一推离洞距离估（< 1.5 m 1 推，1.5–15 m 2 推，
> 15 m 3 推，依据业余进洞率）；开球按第二杆位置；GIR = 第一推是第几杆 ≤ Par−1；球杆按距离对照球包猜，
不再每杆询问；什么都没有默认标准杆。

与现有代码：`WatchAutoShotDetector` 已有站定 / 转动 / 撞击 2 g 三项（撞击门槛对推杆太高）；
需新增：往回找挥动起点 + 累计转角（Apple WWDC23 做法）、推杆 / 切杆分类、撞击时长特征、车速过滤、
同位置聚类、候选无需逐杆确认直接进本场、手表球场包带球道 / 果岭 / 沙坑轮廓。

**门槛全部是起步值**：公开资料没有腕部三分类（全挥 / 推杆 / 试挥）准确率和数据集，撞击时长、车速、8 m 等
都是自定，需要用户戴表实打几场并在复盘逐杆核对作为首批标注。推杆能否稳定检测是最大未知数，所以“按距离估推杆”是常规路径。

## 5. 纠错回流（任何地方的改动都回到算法）

手表本洞成绩、手机计分卡、赛后复盘里的每一次改动都是一条纠错，对回当时的候选动作：

| 改动 | 学到什么 |
|---|---|
| 删一杆 | 该动作是假阳性（试挥、坐车等），相似动作门槛上调 |
| 加一杆 | 找当时被排除的候选改标为真（如撞击 1.7 g 未过门槛），下调门槛；附近无动作则记“没戴表”，不调 |
| 改推杆 | 调推杆门槛，并用真实推杆数修正距离估算表 |
| 改总杆 | 弱标签，按推杆和位置猜最可能错的那一次 |
| 改开球 / 挪位置 | 这块表在该球场的 GPS 偏差；同洞反复出错标记球道轮廓可能不准 |
| 换球杆 | 更新每支杆的实际距离分布 |
| 不改直接确认 | 正确样本，权重最低 |

所有候选（含被排除的）都保留特征值直到这一场复盘不再改动；个人门槛每场最多一小步、同类纠错 ≥ 5 条才动；
纠错只影响以后，不改已确认成绩。先调个人门槛，数据够了再换小模型并做个人微调。原始传感器数据不出手表。

## 6. 业界做法对照（来源见 `watch.html` 第“别人已经怎么做的”一节）

- Apple WWDC23：撞击尖峰 + 往回找起点 + 累计转角；数据约每秒一批，适合事后判断；需锻炼会话。
- Garmin AutoShot：只测全挥杆，推杆 / 切杆明确不测；戴前导手；球车旁试挥打地会误记。
- Golfshot：Series 8+ 自动数推杆（不量距离）；同位置多次挥杆以最后一次为准。
- 18Birdies：只带手机时把“停下来的位置”当候选击球点；旗位 + 第一推位置算第一推距离。
- Arccos：推杆撞击最轻、短推常漏，提供一键加 / 删推杆。
- 微软手环专利：开球 / 切杆 / 推杆分三个分类器。
- 主动学习研究：用用户纠正调模型，平均准确率约 +11%（因人而异）。
- 当时网络限制，多数网站只能看到搜索摘要，原话以链接为准。

## 7. 复盘（`review.html`）

- 单场复盘 = 保存后的本场汇总（同一版式），按钮换成“分享”，多一条每洞累计走势；缺数据不单独提示，缺的格子不显示。
- 每洞落点：全屏 topo；每杆白线 + 编号点（点色 = 落点球位）+ “一号木 221”标签；果岭“推 ×2”；顶部这一洞记分符号；
  底部 18 洞记分符号条，点或左右滑换洞；双击放大、放大后拖动；不显示缓存状态；去掉简洁图切换，只留显示 / 隐藏标签。
- 改杆在同一屏：点“编辑”，编号点变拖柄（拖动带放大镜）；点空白加一杆（插在选中那杆之后，球杆按距离预猜）；
  点一杆，底栏出现球杆（左右滑，猜的那支排第一）/ 球位 / 顺序 ‹ › / 删除；未选中时底栏是推杆和罚杆 −/+。
  去掉排序列表、颜色图例、提示文字和单独的“放大调整”全屏页。保存仍是整洞一次提交，另把每处改动作为纠错交给检测算法。
- 发现：现有“球场回顾”页（RecentRoundReviewView）没有入口。

## 8. 开球前（`pre-round.html`）

- 首页：最大一块随情况变——在球场附近 = 该球场 + 上次的 9 洞组合和发球台 + “开始”（一键开打）；有进行中 = “继续第 N 洞”；
  附近没有球场 = 搜索 + “再打上次那个”。上一场卡片带 18 洞记分符号条；备战、成绩两块。Garmin 同步只在设置里。
- 开始一场：一个球场列表（附近在前带距离，含最近打过的），不再分附近 / 最近 / 本机已下载；9 洞用大块按顺序点
  （角标 1 = 前九，2 = 后九）；18 洞球场只有“全场”；发球台是颜色圆点 + 码数（只打 9 洞时码数减半）；按钮写明组合。
  不显示定位 / 下载状态，未下载也能开打，地图后台补。
- 备战：全屏 topo + AI 球童路线和落点；底部方案切换、这洞球杆顺序、18 洞条；右上换发球台；
  选了就进，没准备好的洞在洞条变淡，先显示 Par 和码数，不弹“地图尚未准备完成”。

## 9. 成绩、表现分析、球包（`stats.html`）

- 成绩：差点估算最大（附近 20 场变化），近 20 场走势（点 = 每场杆数，线 = 近 10 场平均，悬停看某场）；
  近 10 场均杆 / 最佳 / 场数；最近三场带记分符号条；表现分析、球场两块入口（原链接列表收掉）。
- 表现分析：一张从下往上的球洞图（不要卡片墙）——开球左 / 中 / 右比例画在球道上（圆越大越多），果岭中间 GIR、四周没上果岭时
  短 / 长 / 左 / 右（最多的标黄），沙坑旁果岭周边起杆次数，右上推杆每洞几推和三推；左下“最该练”一句；时间范围在右上。
- 时间与频率（对照 GolfLive 要的按季度 / 年 / 频率，现有“时间趋势”内容全部保留）：一张图切逐场 / 月 / 季 / 年；
  季度（或历年）卡片：场数、均杆、最佳、最差、鸟 / 场、双柏+ / 场；打球日历（一格一天）+ 月均场数、最活跃月。点任何一段进那段时间的球局。
- 成绩分布：每 5 杆一根柱（最常见那段实心，点柱进球局）、所有洞的成绩构成、按 Par 3 / 4 / 5（平均多几杆、保帕率）。
  原来藏在进不去的“成绩统计”页里。
- 球场详情：每一场一个点的分布图（GolfLive“分布点阵图”）；27 洞球场按 9 洞（A / B / C）看难洞（每洞平均比标准杆多几杆）；
  组合用 3 × 3 矩阵（行 = 前九，列 = 后九，含同一 9 洞打两遍），格子写均杆和场数、越多越深，没打过为“—”，点格子进球局；
  只打 9 洞的场数单列；18 洞球场不显示组合。
- 成绩首页入口改为四个：表现分析、时间与频率、成绩分布、球场。
- 球包 = 距离阶梯：每支杆 p10–p90 范围条 + 中位线 + 码数，相邻“差 N 码”，< 8 或 > 20 标黄（重叠或空档）；
  点一支可手动改用于球童建议的距离（改过的中位用实心点）或从包里拿掉；“＋ 球杆”加。合并了现在的“成绩 → 球杆”列表、
  “球杆设置”和距离阶梯三处。
- 数据缺口：统计模型里没有罚杆字段；推杆 / GIR / 球道只有时间窗汇总，没有逐场序列。

## 10. 仍待决定 / 后续

- 设置类页面（登录、后端设置、Garmin 会话）按约定用系统默认样式，不单独设计。
- 实现需要的数据：果岭 / 球道 / 沙坑轮廓下发到手机和手表；手表候选挥杆免确认直接入本场的数据通路；
  每洞成绩来源字段；候选特征值的存储与纠错关联。
- 真机效果需在 macOS / 模拟器上渲染确认；本目录原型用低分辨率示例图，旗位界面放大后较糊属正常。
