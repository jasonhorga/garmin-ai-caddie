# AI Caddie · 总 Spec(唯一真源)

**版本 v1.3** · 更新 2026-07-05 · 主线 `integration/v2` · 总体完成度 **~80%**

> 这是**唯一的、活的**产品说明书,**就在主线 `integration/v2` 上维护**(不再散在别的分支)。
> **每次改动都要做四件事:① 记「改动日志」(§5) · ② 更新「完成度总览」(§0) · ③ 维护「待办」(§6) · ④ 有实质进展就把版本号 + 日期往上抬。**
> 用**人话**写:一个不写代码的人也能从头看到尾看懂。技术细节放到文末几份附录里,这里只讲清楚「做了什么、还差什么」。
>
> **一句话大原则(负责人 2026-07-05):数据和准备工作在后端做一次,手机、网页、手表都直接拿来用;每个部件都要能复用;一开始打球,后端就把这局要用的东西提前备好。别一个端一个端地重复修。**

---

## 0. 完成度总览

| 模块 | 完成度 | 说明 |
|---|---|---|
| **网页版重做**(5 个页面 + 外壳 + 真实球洞图) | **100%** | ✅ 已上线,看 `/me` |
| **iOS 重做**(打球界面深色、其余浅色) | **~90%** | ✅ 主要页面都做好了(上架 TestFlight 要负责人点头);还差:球包的「各杆距离阶梯图」 |
| **后端共享层**(球洞图渲染 + 缓存 + 球杆识别) | **~90%** | ✅ 球洞图填满画面 + 认得出球杆,都修好上线了;剩一点收尾 |
| **复盘落点图**(线画粗 + 每个落点标球杆) | **100%** | ✅ 网页 + iOS 都上了:连线加粗、落点只标球杆(距离仍在右侧杆序表) |
| **开局提前备料**(iOS/手表也提前加载) | **~30%** | 🔧 网页版的「提前加载」已上;iOS、手表还没接(下一步) |
| **Apple Watch 重做** | 设计已定 | 🔒 等负责人发话再动手 |

> 版本历史就是下面的「改动日志」(§5),每合并一次就记一笔。完成度跟着 §5、§6 一起更新。

---

## 1. 这是什么产品 · 三个端各干什么

AI Caddie —— 一个高尔夫 AI 球童。对标 Garmin S70 那种手表体验,靠「AI 球童给建议」做出差异。**四条铁律:不显示成功率/百分比、距离一律用码、界面简体中文、断网也能用、绝不编造数据。** 产品名就叫 **AI Caddie**,不显示个人名字。

| 端 | 干什么 | 长什么样 |
|---|---|---|
| **Apple Watch** | 打球的主力(一切以它为原点) | 深色、大数字、地图 |
| **iOS 手机** | 打球时的场上控制台,也能只用手机打完整场 | **打球界面深色**(跟手表一个样);**其它页面(首页/复盘/备战/统计/球包/设置)浅色、原生 iOS 风** |
| **网页** | 复盘 / 备战 / 统计 / 球包(不用来实时打球) | 桌面工作台、亮色底、深色导航栏 |

**几条北极星:** 手表是原点 · 打球深色、复盘亮色 · 数字是主角 · 同一样东西在三端长一个样 · Garmin 的用法 + Apple 的手感。

## 2. 设计系统(细节见附录「设计系统」)

- **颜色**:品牌绿只在三个固定地方用;**成绩颜色按「差几杆」走彩虹 —— Par 是绿色**,好于标准往蓝紫走、差于标准往橙红走,再配上圆圈/方块/三角的形状(色盲也能分)。一套球场配色三端共用。
- **球洞图长什么样(已锁定)= 手绘的精致写实俯视图**:洞要**填满整个画面**、光照固定(太阳在东南、影子朝西北,跟 Garmin 一致)、干净的割草条纹、有树、果岭上有目标圈 + 旗子、只画跟本洞相连的水、边缘描细一点。**图上不烤进打球线、码数、发球台标记** —— 那些是盖在图上面的一层「贴纸」(矢量层),可单独换。样板 `assets/hole-render-topo-prototype.py`,正式版 `ai_caddie/geometry/topo_render.py`。

## 3. 怎么搭的(细节见附录「统一 Spec」附录 C)

- **后端是唯一的数据源 + 唯一画图的地方**:后端把球洞图画成成品图片(画一次存起来),客户端只在上面盖薄薄一层贴纸(路线 / 落点 / 距离 / 球位)。**手表永远不做 3D 渲染**(太耗电),网页大屏才可能上更炫的画法。
- **数据和准备只在后端做一次、三端都来拿**:球洞图渲染 + 缓存 + 提前备料、球杆识别、落点图数据 —— 都后端出,手机/网页/手表直接用;**一开始打球,后端就把这局要用的都备好。**
- 一条法律红线:Garmin 官方那种带签名的地图不能商用,所以底图我们自己画。
- 存储不是问题(几万个球场约 100–175GB);选球场/开局时把离线包提前拉下来。

## 4. 现在到哪了(2026-07-05)

- **网页全套重做 + 真实球洞图**:已上线真站 `/me`。
- **iOS 全部主页面换新皮**:打球深色、其余浅色,已进主线(上架 TestFlight 等负责人点头)。
- **球洞图渲染流水线**:后端有个地址 `GET …/courses/{球场}/holes/{洞}/topo.png`(画一次存起来),还有个「提前备料」接口,三端的球洞图都从这里拿真图。
- **刚修好并上线的两个共享层问题**:① 球洞图以前只占画面一小块、漂在中间 → 现在**填满整个画面**;② 复盘里很多杆显示不出来(显示成 Unknown)→ 现在**认得出你用的哪支杆**,认不出的就干脆留空、不再显示 Unknown。

---

## 5. 改动日志(按合并先后)

| PR / 提交 | 端 | 做了什么(人话) | 状态 |
|---|---|---|---|
| `9c1e93d` `501ef20` | spec | 锁定球洞图长什么样 + 三个 AI 模型对着吵出来的结论;存样板 | 合并 |
| #220 | 网页 | 外壳:导航(复盘/备战/统计/球包/设置)+ 深色栏 + 配色变量 | 合并·部署 |
| #222 | 网页 | 备战工作台(接真实的开球前分析) | 合并·部署 |
| #223 | 网页 | 复盘工作台(落点图 + 每杆顺序 + 计划 vs 实际) | 合并·部署 |
| #224 | 网页 | 统计仪表盘(不编造数据,老老实实显示各环节丢了几杆) | 合并·部署 |
| #225 | 网页 | 球包(各杆距离 + 可编辑,跟 iOS 用同一份) | 合并·部署 |
| #227 | 网页 | 收尾:球场图收起来/条形图按比例/手机上标签不竖排 | 合并·部署 |
| #231 | 网页 | 顶部横条选洞 + 先出占位图再淡入真图 + 提前备料/预取 | 合并·部署 |
| #221 | iOS | 打球/记分界面改**深色**图形化 | 合并 |
| #226 | iOS | 首页/复盘改**浅色**原生 iOS 风 | 合并 |
| #228 | iOS | 备战/统计/球包改**浅色** | 合并 |
| #229 | 后端+网页 | 球洞图流水线(任意洞 + 后端地址 + 缓存 + 网页拉真图) | 合并·部署 |
| #230 | iOS | iOS 三处球洞图接上后端真图 + 兜底 | 合并 |
| #232 | spec | **总 Spec 进主线** + 加版本管理(v1.0)+ 完成度总览;之前只在分支本地没上传→已同步到 GitHub | 合并 |
| #233 | 后端 | **球洞图填满画面**(以前只占一小块)+ **复盘认得出球杆**(认不出就留空,不再 Unknown) | 合并·部署 |
| #234 | 后端 | 让上面那条修复**真的生效**(球洞图存的是旧小图 → 换缓存版本、作废旧图重画) | 合并·部署 |
| #235 | spec | 总 Spec v1.1:**用人话重写**(不写代码也能看懂),更新完成度/日志/待办 | 合并 |
| #236 | 网页+iOS | **复盘落点图**:每杆连线**加粗**、落点**只标球杆**(学 Garmin,不写距离/位置;距离仍在右侧杆序表)。iOS 标签加深色阴影,浅色球道也看得清 | 合并·部署 |
| #238 | iOS | **iOS 复盘逐杆列表补上距离(码)**:原来只有球杆+球位,现在「什么杆 + 打多远」都有(和网页同算法;推杆略)。负责人:没距离复盘没法看 | 合并 |

**怎么上线的**:homeserver 上的 `/me`,绿灯后自动部署(网页改动重新构建 + 后端改动重建 `api` 容器)。iOS 走 TestFlight(等负责人点头,还没发)。
**Spec 网页版**部署后一条命令重新生成:homeserver 上 `bash ~/reserve-specs.sh`(从主线仓库读 md → 渲染 → 放回网页目录),所以就算网页目录被重建冲掉也能一键找回。

---

## 6. 待办

### 进行中 / 下一步
- [ ] **开局统一提前备料**:一开始打球,后端就把整场的球洞图渲好存好(iOS、手表也提前加载);顺便继续把加载弄快。—— 现在网页版能提前加载了,要让 iOS/手表也一样,开局一次性备好整场。

### 尾巴(小事)
- [ ] iOS 球包做成「各杆距离阶梯图」(现在只是一列浅色球杆)。
- [ ] 球洞图底部发球台那头留个小缺口(纯装饰)。

### 预留 · 等负责人完整设计(见 §7)
- [ ] 复盘 v2:手动增/改击球(选位置+球杆+障碍属性)· 缺杆按距离推测 · iOS 复盘满屏+平移+切洞 · 网页只读/手机可改

### 等负责人明说才动(门控)
- [ ] 发 TestFlight · 把 `integration/v2` 合进 `main`(真正发布)· Apple Watch 重做整合。

---

## 7. 复盘 v2 设计预留(负责人 2026-07-05 想法;先占位,完整设计后续出)

> 定调:**复盘每杆至少要「什么杆 + 打多远」两要素**;落点图上的标签保持球杆-only(Garmin 式,干净),距离放在逐杆列表里(职责分离)。iOS 逐杆列表补距离已做(#238)。以下是**预留**,现在不急着做,但数据模型别挡住它们。

**A. 修改 / 增加击球(手动补杆、改杆)**
- 场景:系统可能漏记一杆(没检测到挥杆)→ 用户要能手动**增加**一杆。
- 增加一杆:在图上选**落点位置** + 选**球杆**。
- 记录**属性**:这一杆是否进了障碍区等(见 C)。
- 数据层预留:现在每杆是从 Garmin 导入、只读、按 order 派生;要能改就得给每杆**稳定 ID** + 一层「手动新增/修正」覆盖层(不动原始导入)+ 增/改/删端点。**手机可改、网页只读**(见 D)。

**B. 默认填充:缺球杆时按距离推测**
- 若某杆没记球杆(常见:第一杆),用这杆的**距离**对照你的**各杆距离档案**(median)反推最可能的球杆。
- 标「(推测)」——不假装确定(守「不造假」)。
- 底线:距离能从几何直接算(#238 已给 iOS 补上);球杆推测是**增强**,不是核心。

**C. 障碍 / 球位属性(负责人:这套定义挺重要,先立骨架)**
- 现有球位(已在用):球道 / 长草 / 沙坑 / 水 / 果岭 / 发球台 / 界外。
- 复盘要能标每杆**落在**/**打进**了什么(进沙、下水、OB、罚杆区…),这套要跟**计分(罚杆)**、**统计(各环节失杆)**对齐。
- 先用现有 `lie`/`endLie` 作基础,完整 taxonomy 等负责人设计。

**D. 界面交互**
- iOS 复盘:球洞图要**满屏** + 支持**平移** + **切换洞**(现在是卡片,不满屏)。
- 网页 vs 手机分工:**网页只读**(查看);**手机可看可改**(增/改击球)。
- 负责人「再想一想,后续出完整设计」→ 本节先占位,不动手。

---

## 详细附录(要深挖看这些,本文不重复)
- `2026-07-02-unified-tri-surface-spec.md` — 三端统一设计 + 附录 A(Garmin 数据发现)/ B(续航与竞品)/ **C(渲染与数据架构)**
- `2026-07-03-design-system.md` — 设计系统:§一 颜色 · **§九 球洞图长什么样 + 踩过的坑清单**
- `2026-07-02-garmin-course-data-reference.md` — Garmin 球场数据字典(4 层数据链)
- `2026-07-03-garmin-data-to-features.md` — 从数据到功能的路线图
- `assets/hole-render-topo-prototype.py` — 球洞图样板(正式版 `ai_caddie/geometry/topo_render.py`)
