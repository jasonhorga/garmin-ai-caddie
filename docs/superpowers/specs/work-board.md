# 该做的事 —— 一张清单看全

**更新 2026-07-09**

> 把手头该做的事都列在这一张纸上,免得聊着聊着钻进一件、把别的忘了(你我都会犯)。
> 每件事只写一句话:是什么、下一步干嘛。细节在别的文档里,这张纸只管"还有哪些事、接下来做什么"。
> 规矩就一条:**哪件事有进展,就改这张纸上对应的那一行。**

---

## 四端现状总览(先看这个)

- **网页**:全套重做 + 部署上线,**完成**。
- **iOS**(打球/记分/复盘/首页/统计/球包):主屏全重做 + 复盘编辑全套 + 选发球台,都合并了,**攒着一起发 TestFlight**(见下)。
- **手表**:全套共识计划写好、Fable 审过(#292/#293),**还没建**——这是目前最大的一块。
- **后端/数据/几何**:Garmin 同步数据完整性修好(#288–290);几何按日期回退修好(#294,找回被撤的老球场);这轮又挖出一串几何/数据的活(下面"数据/几何")。

## 现在就能做

### 手表全套共识(最大的一块,计划已就绪)
- 照 5 阶段计划建:**阶段0 数据地基**(果岭坐标+图投影锚点+海水障碍+离线传图)→ **1 主打球屏**(接 #218 真实球道图洞视图)→ **2 交互屏**(选点测距/拖旗/障碍/球道命中/击球列表/旗向/开局流)→ **3 GPS+自动行为**(确认切洞/走动刷新/息屏大字,须真机)→ **4 同步中继**。#218 渲染文件已备进 worktree。计划:`plans/2026-07-08-watch-full-consensus.md`。兜底:有几何→地图 / 无几何→纯记分(现 96/96 有几何,兜底几乎用不上)。

### 数据/几何(这轮抓包 + Fable max 挖出来的,都归到这、按需排期)
- **复盘改用 `/date`**(取"你打那天的球场版本",不是最新版)—— 小,**已采纳待做**。多数场和最新版一样,但语义正确 + 扛未来重测。
- **补海边障碍**:`Ocean/OceanSide/Beach/Cliff`(+`Fringe` 果岭领)—— 数据一直在我们下载的几何里,只是 `export_prodgeometry_hazards.FEATURES` 当初内陆版 MVP 没加。**先全量重扫 104 场**定完整清单再改(这台只有 10 场解码数据)。小、近零成本。
- **旗位纠正**:记分卡 `pinPositionLat/Lon` 经决定性验证 = **Garmin 随附的"果岭中心固定点",不是当天真旗位**(两场相隔4月同洞重合<12米、都在果岭中心)。当"到果岭中心"用,别当真旗位展示。小。
- **果岭 DEM → 坡度/读推杆**:`contourmaps` 是 Garmin DSKIMG 地图,内含**果岭高程网格(DEM)+ 等高线**——**我们唯一的高程源**(自有几何是纯2D)。v1"果岭坡度箭头"便宜,完整读推杆费点;有 mkgmap 现成解码规范。中(~1–2天写解码器)。**净新增、价值最高。**
- **接专用 Hazards 端点**(`courseData/{}/Hazards` protobuf,带类型标签的障碍多边形):Garmin 权威障碍源,**深高被撤 prodgeometry 它照样 200** = 网格断处的稳健兜底/交叉校验。中。
- **更新检查 POST**(`checkForCourseUpdates` 等 3 个):查出"球场被重测/新高程上线",配合"复盘用 date + 检测改造"。小。

## 得你先拿个主意

- **先排哪条?** 手表(最大)vs 数据/几何(一串小的 + 果岭DEM一个中的)—— 想先把哪块推进、什么顺序。我的建议:数据/几何里**小而稳的三个**(date复盘 + 障碍补全 + 旗位纠正)顺手清掉,**果岭DEM单独一条**,**手表**作为主线大块另起。但听你的。

## 你发话才动

- **发 TestFlight 到你手机(攒着一起发)** —— 攒着:复盘编辑全套(加/改/删/拖/放大镜/重排/罚杆)· 选发球台屏 · 球包阶梯图 · 上一场预览卡 · 会员苹果登录。发前 CI 有真模拟器视频/截图先看过。你说发我就 `gh workflow run ios-testflight.yml`。
- **让家里人也能用** —— 后端(各自独立身份/数据隔离/各连自己 Garmin/成员登录)+ iOS(苹果登录、连 Garmin 界面)都做好合并了。差 TestFlight 真机验苹果登录。
- **自动记杆:手表自己认出你打了一杆** —— 研究定了、存档 `swing.html`;你说先搁着。
- **正式对外发布(合到 main)** —— 先不;继续 integration/v2 + 部署 homeserver 自家用。

## 刚做完的(这轮 2026-07-08/09)

- **★ Garmin 同步数据完整性 bug 链**(#288/#289/#290,部署了):只存打完的局(`inProgress=false`)· 删进行中的 · **对账**放弃的(Garmin 删了的本地也删)· 自动填满 4 个被冻在半路的整18洞局。根因:旧逻辑"文件在就跳过",半路同步的局永久冻结。
- **★ 几何按日期回退**(#294,部署了):`/releases/` 取最新版、老场被撤就 404 → 回退 `/date/{那局时间戳}` 取历史版。**找回深圳高尔夫俱乐部 18 洞 = 现 96/96 有几何。**你抓 Garmin app 包破的案。
- **跨终端 demo 录制 skill**(#287):iOS/手表/网页统一"截图→字幕mp4",真录屏为主。
- **手表全套共识计划 + Fable 一致性审计**(#292/#293):5 阶段路线图;校正了"成绩环/实打是保留、阶段0需新契约、GPS全新"。
- 早前:选发球台全栈(#281)· 复盘编辑全套(#273/#276/#280)· 打开即用后端(#263)· 网页全套重做 + iOS 全屏重做 + 真实球道图(部署上线)· 真模拟器视频测试 harness。

---

> 细节文档:总说明书 `spec.html` · 复盘编辑 `edit.html` · Garmin 抱怨 `garmin.html` · 自动记杆 `swing.html` · 手表计划 `plans/2026-07-08-watch-full-consensus.md`。这张纸只管"还有哪些事、下一步做什么"。
