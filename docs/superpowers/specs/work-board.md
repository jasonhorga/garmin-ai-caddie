# 该做的事 —— 一张清单看全

**更新 2026-07-07**

> 把手头该做的事都列在这一张纸上,免得聊着聊着钻进一件、把别的忘了(你我都会犯)。
> 分三拨:**现在就能做的** · **得你先拿主意的** · **你发话才动的**。
> 每件事只写一句话:是什么、下一步干嘛。细节在别的文档里,这张纸只管"还有哪些事、接下来做什么"。
> 规矩就一条:**哪件事有进展,就改这张纸上对应的那一行。**

---

## 现在就能做

- **手表重做** —— **不照 #218 原样合**(它那套洞图计分环 + 64%/PlaysLike 被你后来的三端统一 spec 推翻了、还撞不造假)。**按统一 spec 另起一份**(去计分环、去编的数、恢复大数字):得先出一版设计,再落 SwiftUI(CI 编译+快照+**现在有真模拟器视频**可验)。这是目前最大的一块。

## 得你先拿个主意

- (这一轮的决策都问完了。新的等冒出来再放这。)

## 你发话才动

- **发 TestFlight 到你手机(攒着一起发)** —— 你说等 iOS 都做完一起发一版。现在攒着的:**复盘编辑全套**(加/改/删/拖/放大镜/重排/罚杆)· **选发球台屏** · 球包阶梯图 · 上一场预览卡 · 会员苹果登录。发之前 CI 已有真模拟器视频/截图先看过一遍。你说发我就 `gh workflow run ios-testflight.yml`。
- **让家里人也能用** —— 后端(各自独立身份/数据隔离/各连自己 Garmin/成员登录)+ iOS(苹果登录、连 Garmin 界面)都做好合并了。差 TestFlight 真机验一遍苹果登录(你先不发)。
- **自动记杆:手表自己认出你打了一杆** —— 方向+可行性都研究定了、存档在 `swing.html`;你说先搁着。想认真做(要真机采数据、训模型)了说一声。
- **正式对外发布(合到 main)** —— 你说先不;现在自家/家人用,继续 integration/v2 + 部署 homeserver。

## 刚做完的(留最近几件)

- **★ 真模拟器视频测试 harness**:CI 里 `simctl recordVideo` 边跑 XCUITest 边录屏 → 真机前能**看真 app 跑起来 + 点/拖的过程**(mp4 artifact),不只静态快照。加了复盘编辑/选发球台的 XCUITest 流程。**测试策略**:编译+静态快照(样子)→ 真模拟器实景截图+视频(真跑/导航/点得动/不闪退)→ 剩手指精度/GPS 真机验。让 TestFlight 从"抓 bug"变"最后确认"。
- **★ 选发球台 全栈**(#281,后端+网页已部署):`GET /api/v2/courses/{id}/tees`(真台真码,如 gid31669=Black6683/Blue6262默认/White5765/Gold5380/Red…,缺几何留空不造假)+ iOS 开局选台屏(穿 tee_box 像 nine)+ 网页选台下拉。iOS 走 TestFlight。
- **★ 复盘编辑 PR2**(#280,合了):拖动改落点 + 放大镜 loupe + 落点列表手动重排(+ 编辑锁横滑)。**复盘编辑至此全套落地**(后端 #273 + 核心闭环 #276 + 手势 #280),就等 TestFlight 试手感。
- **复盘编辑 iOS 核心闭环**(#276,合了):满屏图上「编辑」进出、点空白补一杆(弹框选杆/球位)、点落点改球杆/球位/直接删、每洞罚杆 +/− 条、永不变砖;乐观本地先落、后台落库。native-mobile 编译+快照绿。真拖动手感等 TestFlight。
- **球洞图发球台小 tee 台标记**(#275,部署了):route[0] 投影处画浅绿修剪台面 + 门柱,沿击球方向,和果岭旗呼应;肉眼验两洞位置对(避开上次画到天上的坑)。
- **★ 复盘编辑「后端」上线**:点地图加杆(反投影+插到前后两杆之间+自动连线,空洞也能加=永不变砖)· 拖动改落点 · 落点列表重排 · 像素↔世界坐标逆投影,1391 测试绿(#273),已部署 homeserver。
- **Garmin 抱怨汇编**:系统扒了 Garmin 手表+App 大家骂什么/想要什么(`garmin.html`)——最响是「统计太浅」;纠错:Garmin 能两杆间插杆。**复盘编辑设计定稿**(`edit.html`)+ 后端实现计划,都照 Garmin 参照定的。
- **★ 部署上线**:积压的 61 个提交一次性部署到 homeserver(复盘修改 / 打开即用 / 网页中文化+provenance+去重叠 全套)· DB 迁移干净跑完 · **定时同步提到每小时**(同步 :37 + 自动预热最近一盘 :45)· funnel /me 200。
- iOS 首页「上一场」真实球道预览卡(#268,随下次 TestFlight 上)· 复盘网页只读展示修正(#265)· 网页球场分布图气泡去重叠(#267)。
- **「打开即用」后端核心**:数据一落地自动备好最近一盘(预热球洞图 + 烤统计)+ shotmap 底图改用缓存图(复盘秒开、iOS 免改),1378 测试绿(#263)。
- 复盘落点图**球杆标签避让**:近距离两杆的标签不再挤成一团(#258)。
- 手机「球包」页做成了**各杆距离阶梯图**(按距离长短排、横条成正比、没距离的留空,#253)。
- 网页**去英文残留 + 方向词中文化**(偏左/偏右/偏短/偏长)+ 报告页几处英文转中文(#254)。
- 手表距离核实早就全按「码」显示了——过期 todo,撤掉。
- 复盘改造**后端地基**:每杆一个稳定身份证号 + 一本按人隔离的"修改记录本"(增/改/删/手填罚杆,原始数据不动),1370 个测试全绿(#250)。
- 自动记杆到底能不能做的调研 + 找两个 AI 帮着狠挑毛病。
- "试挥和真打怎么分"那处更正。
- 手机复盘每一杆补上了距离;一开局就把整场球洞图提前备好。
- 复盘的落点图:连线加粗、每个落点标上用了哪支杆。
- 球洞图铺满整个画面、认得出你用的球杆。
- 网页全套重做、手机所有页面换了新样子 —— 都上线了。

---

> 想看细节:总说明书 `spec.html` · 复盘改造 `rv2.html` · **复盘编辑 `edit.html`** · **Garmin 抱怨汇编 `garmin.html`** · 自动记杆 `swing.html`。这张纸只管"还有哪些事、下一步做什么",不放细节。
