# 该做的事 —— 一张清单看全

**更新 2026-07-10**

> 把手头该做的事都列在这一张纸上,免得聊着聊着钻进一件、把别的忘了(你我都会犯)。
> 每件事只写一句话:是什么、下一步干嘛。细节在别的文档里,这张纸只管"还有哪些事、接下来做什么"。
> 规矩就一条:**哪件事有进展,就改这张纸上对应的那一行。**

---

## 四端现状总览(先看这个)

- **网页**:全套重做 + 部署上线,**完成**。
- **iOS**(打球/记分/复盘/首页/统计/球包):主屏全重做 + 复盘编辑全套 + 选发球台,都合并了,**攒着一起发 TestFlight**(见下)。
- **手表**:**数据地基 + 整个「洞视图」建完了(阶段0 + 阶段1,#288–304 共约12个PR,都合并、CI+快照逐个验过)**。手机在旁(companion)时,手表已是完整的高尔夫设备:真几何球道图(topo底图+你/旗/留杆/球童线)、「你」随手机GPS实时平移、果岭前/中/后距离随走动刷新=真测距仪、计分环+实打(按owner 2026-07-08保留)、大字模式(点一下抬手一眼)、无几何洞回退大数字距离、外加原有的计分/计分卡/选洞/球童/障碍/离线同步。**剩阶段2(交互屏)+ 阶段3(手表自带GPS的独立模式)+ 阶段4(同步中继)——多需真机触摸/GPS验证,是大型多会话弧**(下面"手表剩余")。
- **后端/数据/几何**:Garmin 同步数据完整性修好(#288–290);几何按日期回退修好(#294,找回被撤的老球场);海边障碍补全(#296);这轮还挖出几个几何/数据的活(下面"数据/几何")。

## 现在就能做

### 手表剩余(阶段0+1 已完成,见上;剩这些)
- ✅ **阶段0 数据地基** 完成(果岭坐标+图投影锚点+海水障碍+离线传图,#288–300)。
- ✅ **阶段1 洞视图** 完成(#301–304:真几何地图+live你+测距+计分环/实打+大字+无几何回退)。
- ⏳ **阶段2 交互屏**:✅ **球道命中问法 #328** 已做(手表输入屏加「上球道」左/中/右/没上 → 全链路 fairway 事件 watch→phone→后端 round_ingest,归约 hit/miss 喂 开球上球道 统计、显式赢 GPS 派生);**击球列表**诚实卡前置(手机 live round 现只存当前选杆+分数聚合、不追踪逐杆明细,做列表要先给手机加逐杆追踪=更大数据模型改动);选点测距·拖旗·旗向(实时触摸)**需真机**。
- ⏳ **阶段3 独立模式**(手表脱离手机自带 GPS):要 CLLocationManager + HKWorkoutSession(后台/息屏)+ **提前把整场球场几何缓存到表**——**净新增、模拟器/CI 无法验、只能真机 TestFlight**。companion 模式已覆盖"手机在旁"的常见场景,独立模式是"手机放包里"才用到。
- ⏳ **阶段4 同步中继**(手表→手机→云,当前是手表打完直传云——能用,中继只是连通性优化,低优先)。
- 计划:`plans/2026-07-08-watch-full-consensus.md`。#291(WatchDistanceHero)已并入阶段1f。

### 数据/几何(这轮抓包 + Fable max 挖出来的,都归到这、按需排期)
- ✅ **复盘/开新局的几何按日期回退**(取"你打那天的球场版本")—— #294 已做(找回被撤的深高等老场,几何覆盖 96/96)。
- ✅ **补海边障碍**(#296 代码 + 2026-07-10 重扫):homeserver 全量几何上把 **72 个海边洞** hazards 重导一遍,全部补上 water/water_edge/bunker(Ocean/OceanSide/Beach);prep 缓存按 HAZARD_DIR 指纹自动失效即时生效。
- ✅ **旗位纠正**(#325):审计后发现 **v2 产品里根本没把这点当真旗位展示给用户**(只内部当"到果岭中心"距离用)—— 落"无误标"分支,加了澄清注释 + 语义测试防未来误用。
- ✅ **果岭坡度/读推杆**:v1 坡度箭头早已做(#308/#309/#311,**从 mesh 3D 高程算、不需 DSKIMG**——"自有几何纯2D"那句过时了);**完整读推杆 #326** 也从同一 mesh 拟合平面出上下坡 + 左右曲 + 强度 + 中文摘要,太平/太稀诚实兜底。
- ✅ **专用 Hazards 端点客户端**(#327):`securemaps.garmin.cn/golf/courseData/{}/Hazards` client + 推断解码器 + fixture 测试;**活体被 `garmindlm` URL 签名墙挡(403)**,拿到 app 抓的签名 URL / 有效 cookie 即可上线(解码器 field-number 容错、调整小)。
- ✅ **更新检查客户端**(#327):`POST omt.garmin.cn/CourseViewData/checkForCourseUpdates` **请求已实测通(匿名 200)**;响应是空 diff(需一个已知历史 release 串才吐行),响应解码按 CourseView 家族推断 + fixture 测试。配合"复盘按打球日期取对版几何"。

## 得你先拿个主意

- **手表 Phase 0+1 做完了(companion 洞视图整套),下一步排哪条?** 三个方向:(A)**先发 TestFlight 真机验手表**——CI/模拟器验不了实时 GPS/触摸,上你的真 Apple Watch 才看得到"你"随走动平移、果岭距离刷新、大字;(B)**继续手表 Phase 2-4**(交互屏 + 独立模式 GPS)——多需真机 + 各是子活,是大型多会话弧;(C)**转去数据/几何**(果岭DEM坡度=价值最高的净新增、旗位纠正、障碍重扫)。**我的建议:先 A(TestFlight 真机验已建的),再按你的感受决定 B 深入手表 还是 C 转数据。** 但听你的。

## 你发话才动

- **发 TestFlight 到你手机(攒着一起发)** —— 攒着:复盘编辑全套(加/改/删/拖/放大镜/重排/罚杆)· 选发球台屏 · 球包阶梯图 · 上一场预览卡 · 会员苹果登录。发前 CI 有真模拟器视频/截图先看过。你说发我就 `gh workflow run ios-testflight.yml`。
- **让家里人也能用** —— 后端(各自独立身份/数据隔离/各连自己 Garmin/成员登录)+ iOS(苹果登录、连 Garmin 界面)都做好合并了。差 TestFlight 真机验苹果登录。
- **自动记杆:手表自己认出你打了一杆** —— 研究定了、存档 `swing.html`;你说先搁着。
- **正式对外发布(合到 main)** —— 先不;继续 integration/v2 + 部署 homeserver 自家用。

## 刚做完的(这轮 2026-07-08/10)

- **★ 数据/几何 + 手表 Phase2 一批(2026-07-10,用户「1-7 做完」清单)**:6/7 落地——**pin=果岭中心澄清 #325**(审计发现没误标,加注释+测试)· **果岭 mesh 读推杆 #326**(green_read 上下坡+左右曲+强度+摘要)· **Garmin 更新检查+Hazards 客户端 #327**(请求实测/响应+签名墙阻碍诚实记录+fixture 测)· **海边障碍重扫**(homeserver 全量几何,72 洞补 water/edge/bunker)· **手表 上球道问法 #328**(全链路 fairway 事件→round_ingest,显式赢派生)。**并行 5 个隔离-worktree 子代理 + 我协调/CI 验/合并**;子代理 3/5 认真交付、手表那个 no-op 两次→DIY(记忆教训)。**唯一未做=击球列表(item1),诚实卡点:手机 live round 不追踪逐杆明细、需先加数据模型**。
- **★★ 五段真数据演示录屏(带中文字幕,都在 funnel `/demos/`)**:网页(真数据·真几何落点图,用 `?admin=` 绕苹果登录墙对真 owner 数据录)· iOS 全流程 · 手表边走边测距 · 手表点球道量距 · **手机↔手表联动**(手机经 WatchConnectivity 推实时距离、手表随走动刷新 190→29 码——#322 配对模拟器 CI lane,盲搭一次成;证实配对模拟器的 WC sendMessage 在 macOS CI 上通)。管线记在记忆 `demo-video-pipeline`。
- **Fable 视频复核 → 修好它挑出的所有问题**:手表测距气泡压球童卡(#320 clamp)· 统计页「问题引擎/尚未计算」黑话 + 球包页英文术语去掉→人话(#321,部署)· 网页 mock 空落点图/黑尾/字幕→改真数据重录。教训:成品要过一遍对抗式视频复核,别自认为干净。
- **「修啊」三件 app 修复(都合并、部署上线)**:复盘落点图双「完成」→关闭/编辑分开 + 实战屏黑图加载态(#319)· 备战/实战首开慢的 **prep 后台预热**(#318,`_prewarm_course_prep`,秒开)。
- **修 cache-warmup flaky 测试(#323)**:根因=app lifespan 的后台守护线程(stats-cache-warm/prepare-recent-boot)泄漏进精确计数 spy(#316/#320/#322 都被它红过);测试 setUp 里 drain 掉,repro 证实 5→3。以后不再无故 red PR。
- **★★ 手表洞视图整套(阶段0+1,#298–304)**:数据地基(果岭经纬度+topo图投影锚点+离线传图)→ 打球时手表渲染**真几何球道图**(手机推干净 `/topo.png` + 预算的像素锚点,手表零投影数学直接画:topo底图+球童线+留杆+旗+你)。**companion 测距仪**:「你」随手机GPS投影实时平移、果岭前/中/后随走动 haversine 刷新。**大字模式**点一下抬手一眼、**无几何洞**回退大数字距离。**计分环+实打**按你 2026-07-08 的意思保留(我一度误砍、#303 修回)。每屏 CI 快照肉眼验过。
- **★ Garmin 同步数据完整性 bug 链**(#288/#289/#290,部署了):只存打完的局(`inProgress=false`)· 删进行中的 · **对账**放弃的(Garmin 删了的本地也删)· 自动填满 4 个被冻在半路的整18洞局。根因:旧逻辑"文件在就跳过",半路同步的局永久冻结。
- **★ 几何按日期回退**(#294,部署了):`/releases/` 取最新版、老场被撤就 404 → 回退 `/date/{那局时间戳}` 取历史版。**找回深圳高尔夫俱乐部 18 洞 = 现 96/96 有几何。**你抓 Garmin app 包破的案。
- **跨终端 demo 录制 skill**(#287):iOS/手表/网页统一"截图→字幕mp4",真录屏为主。
- **手表全套共识计划 + Fable 一致性审计**(#292/#293):5 阶段路线图;校正了"成绩环/实打是保留、阶段0需新契约、GPS全新"。
- 早前:选发球台全栈(#281)· 复盘编辑全套(#273/#276/#280)· 打开即用后端(#263)· 网页全套重做 + iOS 全屏重做 + 真实球道图(部署上线)· 真模拟器视频测试 harness。

---

> 细节文档:总说明书 `spec.html` · 复盘编辑 `edit.html` · Garmin 抱怨 `garmin.html` · 自动记杆 `swing.html` · 手表计划 `plans/2026-07-08-watch-full-consensus.md`。这张纸只管"还有哪些事、下一步做什么"。
