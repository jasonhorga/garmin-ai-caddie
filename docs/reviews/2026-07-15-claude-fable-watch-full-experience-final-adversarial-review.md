# Claude Fable 5：Apple Watch 全体验收口对抗审查

> 日期：2026-07-15 UTC
> 性质：纯 Fable、只读、max effort、无 fallback 的独立最终裁决
> 状态：REVIEW INPUT；由 Codex 逐条接受、修改或否决后进入联合文档
> 任务会话：ffe8b3db-ed5e-4619-99f9-af6ff7ba75e2
> 结果：completed
> 模型纯度：modelUsage 仅 claude-fable-5
> fallback 禁用：CLAUDE_CODE_DISABLE_REFUSAL_FALLBACK=1
> 文件修改：Fable 本身未修改任何文件

> **2026-07-16 AUTHORITY CORRECTION：**本文是历史 review input，不再定义当前 Owner 队列。其“旧文档降级需要 Owner 确认”结论在核查旧稿无 Owner 批准记录、且与 L01–L03/L07 冲突后已被推翻；当前权威结论见[决策账本](2026-07-15-watch-decision-and-task-tracker.md)与[纯 Fable 队列终审](2026-07-16-claude-fable-watch-owner-decision-queue-final-adversarial-review.md)。本文关于成绩环的后续证据更正仍有效。

> **2026-07-15 后续证据更正：**本审查关于“不得静默推翻用户成绩环保留决定”的治理判断正确；但其沿用的上游研究漏掉了 Garmin 官方 `Score History`。S70 确实具有“实体 1–18 洞刻度 + 屏内逐洞成绩色段”。因此 A1 的默认 KEEP 直接恢复，不再申请用户重开；凡本文后续把“成绩环移出根页”列为建议或用户门的内容，均由 [S70 成绩环证据更正](2026-07-15-s70-score-history-ring-evidence-correction.md) 取代。

## 运行审计

- 模型：claude-fable-5
- effort：max
- terminal_reason：completed
- outputTokens：46672
- cacheReadInputTokens：1273209
- cacheCreationInputTokens：117599
- costUSD：5.959048999999999
- permission_denials：空
- fast_mode_state：off
- modelUsage 中未出现 Opus、Haiku 或其它 fallback 模型

补充：此前更大的全景任务 adfedb95-cdac-493e-8c48-8c49d5412458 同样只有 claude-fable-5，但在约 102 分钟后 Request timed out，没有可用最终正文；其纯度通过、完成度失败。本文件来自随后启动的聚焦收口任务。

---

核验完毕。以下是收口版最终裁决。先交代证据基础：我把联合草稿里承重的仓库断言逐条对了源码，**抽验 17 条全部属实**（三套互斥入口、`seedRound` 无生产调用、legacy 分支不渲染 topo、`saveActiveHole()→goToNextHole()`、`selectHole` 污染 activeHole、无后端配置时 `confirmFinish()→finishLocally()→store.clear()` 连 pending 事件一起清、上传按整批 eventIds 一次记成功、全 target 零 `digitalCrownRotation` 却画着「转表冠缩放」、`yardsPerPx` 像素测距、拖旗/测距只是视图本地 @State 无持久化、`youPxOverride` 使 holeMap.you≠tee、存储为 JSON 快照原子覆盖非 WAL、`WatchInputKind` 仅五种、`playsLikeDelta`（米）在 `WatchHoleMapView.swift:206-208` 直接与码数相加、`showPlaysLike: s.elevationDeltaM != nil` 有高差即默认开、中距黄色仅在实打态、`fairwayResult` 字段有而 UI 无、distance 事件双映射路径并存）。**草稿的代码事实层可以当地基用，这一点 CONFIRMED。** 问题集中在四类：对用户既有决定的静默推翻、对 Round 2 未决项的过度收敛、§9.4 的事实/推断混标、若干工程量与漏项。

---

## A. 对联合草稿的逐条攻击

**A1（最重）· 静默推翻用户已做过的保留决定 —— 18 洞成绩环。** 2026-07-08 用户在 Fable 审计后明确裁定「成绩环保留」（外圈洞视图、放大时隐藏）。总稿 §4.3 把「18 洞成绩环」列为「不应常驻」、§13.2 裁决「移出根页，重新验证」，全程没有一个字提示这是在推翻用户本人的决定。§20 只有「联合可直接定」和「原型再定」两个队列，没有「必须用户亲自确认」队列，成绩环就这样被归到联合可决范围里了。CONFIRMED（记忆记录 + 文档原文冲突）。原裁决是“申请用户重开、默认维持”；**后续官方证据进一步确认 S70 本身就有该环，因此现在无需申请重开，直接维持 Owner KEEP。**

**A2 · 同类问题 —— 地图上的球童路线。** 当前已批准的洞图设计里 you→layup→green 的球童线是画面组成部分（`WatchHoleMapView` 实线+虚线两段，快照经用户看过定稿）。§4.3「所有 AI 路线」不应常驻，等于把它从根页洞图上拿掉。DERIVED（用户对路线本身没有像成绩环那样的显式保留记录，但它属于已过目定稿的视觉）。裁决：同 A1，进用户确认队列；可行折中（路线只在 Map Detail/球童面出现、根页洞图保持干净）作为提案一并呈上。

**A3 · 过度收敛 —— Round 2 的头号未决被无声关闭。** Round 2 的三候选是 K1（表冠=显式缩放）/K2（表冠=目标停点轴）/K3（Apple 原生栈），并把「K2 存亡=表冠焦点仲裁与目标轴可用性」列为三项最大未决之一，明确说只能同场原型裁决。总稿 §3 的方案 A/B/C 不是 K1/K2/K3：A 把 K1、K2 合并且 §4.4「进入 Map Detail 后表冠成为缩放」默认了 K1 的语义，B≈K3，C 是 Round 2 里根本不存在的三页复活稿。K2 的目标停点轴从此在全文消失，§20「原型再定」清单里只剩「根页表冠是否完全空」。这是没跑原型就淘汰了前一轮明文「待测」的方案，且丢失了决策出处。DERIVED。裁决见 B 节。

**A4 · 事实/推断混标 —— §9.4「已核验的进取结论」。** CMBatchedSensorManager 的能力、「Series 8/Ultra 一代起」、「Golfshot 等量产产品证明旧设备 100Hz 路线可行」、「批量延迟不妨碍击球后 0–1 秒级 Club Prompt」这四条都不在证据包里、没有任何引用来源，却顶着「已核验」标题。按证据包自己的纪律：前两条应标 PLATFORM（可查 Apple 官方文档但本轮未附证）、Golfshot 应标 MULTI-SOURCE 待补来源、「0–1 秒」是乐观 DERIVED——批量数据本身约按秒级到达，加检测与呈现仲裁，可承诺的上界是「击球后 ≤3 秒」，且实际何时弹还受浮层仲裁控制。UNKNOWN 待 spike 实测分布。

**A5 · 工程低估 —— §8.3 fairway 默认推断重新引入了 Round 2 已撤回的自动预填，且没写数据依赖。** 推断链是「第二个 shotStation 的 startLie + 球洞中轴」，比 Codex 反对过的落点 L/C/R 聪明（不需要球道边界，只需中轴左右），**但 startLie 的分类本身需要地表面数据**（球道/长草/沙坑多边形）。后端几何有这套面（`SURFACE_PRIORITY` 含 fairway/rough/bunker 等，CONFIRMED），手表离线包契约里没有。要么整轮包携带 surface 数据（体积待量化），要么推断放到手机/服务端事后算、Watch 只显示结果。总稿必须写明这条依赖，否则「推断只是默认选项」看起来免费，实际是一条新契约。

**A6 · 内部矛盾 —— 低电在 §12.1 排进模态队列第 3 位，与 §6.6 的分层冲突。** §6.6 明确交易浮层槽与状态警告层分离、GPS 异常走并行状态栏，低电却被塞进同一条抢占序列。裁决：低电默认走并行警告层；只有需要用户做决定时（例如「电量将尽，关闭 AutoShot 省电？」）才进交易槽，位次高于 Club Prompt、低于成绩归属。DERIVED。

**A7 · 漏半条 —— 洞末差额只写了 +1。** §10.5 的差额入口只处理「总分比账本多 1」；Round 2 §2 算例 3 的负差额（账本多记 → 提示清理误报，入口=击球列表）在总稿里没有对应条目。补上，且两者出现时机都写死为「成绩确认完成后与计分卡内」，不做场中弹窗。

**A8 · 工程低估 —— 「新事件=全栈契约变化」在 Track 划分里没有名字。** Round 2 §12 写透了 shot/holeScore/holeAdvance/pinSet 触及 Swift 模型、后端 Pydantic、JSON Schema、reducer/replay、统计与 Web/iOS 消费端；总稿 §14.4/§15 的八条 Track 里，后端契约、幂等/死信、修正后统计重算这些工作没有归属，读起来像后端免费吸收。同理 §14.2 把双通道同步压缩成一句「统一 outbox、多层 ACK」，应把 Round 2 §12 的路由租约、三层 ACK（watchPersisted→handedOffToPhone→serverAccepted/Rejected）、「全集 serverAccepted 才转 synced」原文搬进来，而不是靠一句概括。

**A9 · 可以现在封死的复杂度尾巴 —— ResolutionEpisode 连环债。** §19.12 自认「连续漏确认两洞可能变成复杂链，需要多洞专项设计」。不需要专项：加一条上限规则即可——任意时刻至多一个未决 episode；第二次换洞候选出现时，旧 episode 自动降级为纯 scoreDebt（保留欠分、解绑杆归属，杆按 holeAtCapture 留在原候选洞、可事后 correction）。DERIVED，成本一句话，删掉一整类状态爆炸。

**A10 · Club Prompt 的表冠依赖没有兜底声明。** §11.2「表冠滚动球杆」依赖浮层借走/归还焦点，这在平台矩阵里是 UNKNOWN。必须写明：点按可完成全部操作（选杆/跳过/误检），表冠只是增强。

**A11 · 小事实误差。** §5.1 第 3 步「一场时可自动建议」——官方行为是「附近仅一场则自动选定」（OFFICIAL），照抄即可，不必弱化成建议。另外 §7.1 快速接受首屏还要放「推荐依据与置信摘要」，在 41mm 上有挤爆风险，压到一行以内、详情点开。

**A12 · 开局流程没有标注每一步的归属面。** §5.1 读起来像 Watch 独立完成选场（「GPS 搜索附近球场」），而 §13.1 的基线是「手机准备、Watch 独立打完」，附近球场搜索在 Watch 上并不存在。每一步要标「腕上可完成 / 需手机 / 需网络」，否则「Watch 独立」的边界又会被读者脑补。

其余主体（三链独立、candidateShot 语义、Tee last-wins、约 10 码实证口径、打厚了唯一拆分、超时不写推荐杆、结束不清空、editingHole 分离、六组状态、§13 全景矩阵、§14 复用/淘汰清单）：**攻击后站得住，接受**。S70 事实层与证据包逐条比对无漂移（Finish 四出口、起始洞 30 米仅用于起始洞判定、Big Numbers 为持久模式等全部对齐）。

---

## B. 三页 / 五页 / 单根页 终审

- **五页：淘汰，终审通过。** CONFIRMED 级依据三重：S70 官方是「一个当前洞 + Golf Menu 仪表面」而非兄弟页（证据包 §1.6）；任务频率表里高频读距与低频计分不同级；四份文档独立收敛无一反对。07-10「操控宪法」就此降级为历史输入，这件事本身要落一条 decision（见 F）。
- **单根页：通过为唯一正式 IA。** 单一当前洞根 + 浅推入仪表面 + 单一交易浮层 + 并行状态层 + 持久待处理队列。附加两条不可省：①**工具入口可发现性是这套 IA 的头号残余风险**（Apple Watch 没有 S70 Action 键），首轮田测必测「新用户三洞内能否自行找到 Green View/计分」；②根页表冠空 vs 直接缩放，维持 UNKNOWN 待原型。
- **三页：不是产品方向，且比总稿再收紧一档。** 总稿说「保留为可交互对照」；我裁决**不投可交互原型预算**——§20 的全部待测项（入口可发现性、表冠、AOD、换洞默认档）没有一项需要造一个三页原型来回答。只留纸面对照；仅当 Track A 田测证明单根页入口不可发现（其明示失败模式）时重启评估。
- **修正 A3：恢复 K2 的决策痕迹。** 在 Map Detail 原型里把「目标停点轴」作为表冠语义的对照臂与「显式缩放」同场测（一个构建、切换配置即可，成本近零），并在 decisions 里记录「Round 2 曾列 K2 为同等候选、总稿默认 K1 语义的理由=S70 忠实 + 平台风险最低」。DERIVED：我同意 K1 是默认假设（S70 官方就是 slider 缩放 + 点选目标，K2 是自创交互且踩在最大的平台 UNKNOWN 上），但淘汰要靠原型数据，不靠合稿时的沉默。

---

## C. AutoShot「第一版即 Beta」：**接受，附四处修改与明确技术边界**

接受的核心（与用户锁定「不得因困难无限后置」一致）：规则状态机 + 置信分级 + 高置信才弹 Club Prompt；前导腕、非推杆、完整挥杆为首版承诺范围；手动记杆/补杆/误检删除/改杆永久存在；先落盘后触觉后 UI；不整轮持久化原始高频数据；中低置信不改成绩、不换洞、不写实际球杆。这套设计成立。

必须修改：
1. **时延承诺 0–1 秒改为 ≤3 秒**（A4；实际弹出时机仍受呈现仲裁控制，数字待 spike 实测后收紧）。
2. **旧设备 100Hz 路径降为第二批**。它目前唯一的「可行性证据」是 Golfshot 传闻级引用，未入证据包。首批只承诺高频设备（watchOS 10+ / Series 8+/Ultra，UNKNOWN 待 spike 核实确切机型线），旧设备走影子采集 + 明确的数据门与时间表。这不是无限后置：首批设备立即出 Beta，旧设备有写死的升级条件。
3. **续航成为 Beta 发布门的一部分**。高频采样 + GPS + Workout 的五小时曲线 spike 必须在对外承诺前跑完；若不成立，降级策略（如按球位场景分时段采样）要先设计，不许现场发明。UNKNOWN。
4. **高置信阈值按设备/佩戴腕分开标定，标定期一律影子**；推杆、trailing wrist、轻切在 Beta 文案里写「不支持」，不写「即将支持」。

明确技术边界（写进 spec 的硬约束）：需要活动 Workout 会话；不能用低频先检测再临时开高频（冲击已过去，CONFIRMED 为物理事实）；GPS 取击球时刻邻近的历史 fix 而非批量到达时位置；Motion 与 Location 时间轴统一；仅 opt-in 保存少量压缩窗口；同一时间只能有一个 workout，被抢占时明确降级；HealthKit/Motion 权限被拒时 AutoShot 不运行、测距记分照常。发布门指标按设备/佩戴腕/左右手/杆型/球位/步行球车/GPS 质量分列，数字先取基线再冻结（维持总稿 §9.7）。

---

## D. 四项最终裁决

**打厚了三入口：接受。** 上一杆热区 / 洞末 +1 差额 / Golf Menu→本洞击球三入口成立；§10.6 否决「每次近距二次冲击弹二选一」维持（与用户锁定「唯一拆分语义是打厚了、不做 OB/Mulligan/暂定球恢复 UI」严格一致）。修改两处：补负差额的「清理误报」镜像入口（A7）；差额入口的出现时机写死在成绩确认完成后与计分卡内。恢复语义维持原文：前一杆起点=被覆盖 observation 位置、终点=下一击球位置、correction 追加不删原始、不自动改已确认总分。CONFIRMED 与用户锁定完全对齐。

**Club Prompt：接受。** 只补杆号；跳过=club unknown 且击球保留；误检是独立显式动作（下滑≠误检，Round 1 的矛盾已在 Round 2 §8 修正并被总稿继承）；推荐杆永不自动写入；超时只从浮层可见且屏幕亮起计、超时仅收起不提交。修改一处：点按必须完备，表冠只是增强（A10）。

**成绩确认：接受。** 快速接受（一次原子写入，不再追问）+ 完整确认（总杆→推杆→Par 4/5 的 HIT/LEFT/RIGHT→罚杆→复核）双路径；scoreRecommendation 是带置信/证据/失效原因的推断对象，未点击永不转正；fairway 无可靠证据保持缺失、不造 HIT；推杆只做统计不重复计入（Round 2 §2 的闭合模型与三算例是这套的数学地基，总稿继承正确）。「任意时刻可修改任意洞」由 activePlayHole/editingHole 分离保障——当前代码确实做不到（`selectHole` 改 activeHole 且保存后强制进下一洞，CONFIRMED），重设计判决维持。修改：快速接受首屏的证据摘要压到一行。

**换洞与中断优先级：接受，两处修改。** 确认式小签为 v1 默认（高置信自动为原型对照、S70 式全自动为用户开关，三档比较表维持）；到下一 Tee ≠ 换洞、三链独立维持；下一洞首杆先静默落盘为 provisional、抬腕优先呈现上一洞成绩确认、快速接受=「确认成绩+接受推进+归属候选杆」三合一原子操作、「继续上一洞」=reject 推进+杆归上洞+不自动确认——与用户锁定语义逐条吻合，CONFIRMED。修改：低电改并行警告层（A6）；补「至多一个未决 episode」上限规则（A9）。优先级表其余维持：Club Prompt 永不覆盖成绩确认；AOD 只投事实层；触觉一律在本地持久化之后。

---

## E. 完整 S70 旅程漏项检查

用户点名的十一段（开局、地图、Green View、PinPointer、Big Numbers、AOD、雨天、暂停、结束、同步、赛后）在总稿里全部有入口、状态与验证归属，无整段缺失。真正的漏项与半漏项：

1. **差点/净杆（Handicap Scoring）完全没出现**——S70 有此能力，Big Numbers 的滑动页里也带 handicap（证据包 §1.11）。裁决：v1 不做可以，但必须作为一条明确的 decision（理由：个人深统计产品，净杆价值低）写下来，而不是无声缺席。
2. **不记分模式 × AutoShot/Club Prompt 的关系没定义**。S70 里 AutoShot 依赖开启记分；我们的产品立身是深统计，应当解耦——不记分也可记杆、Club Prompt 照常。需要一行明确裁决。DERIVED。
3. **多人记分**：明确「只记佩戴者本人」，进 decisions。
4. **整轮跨洞击球列表**（S70 Measure Shot 的 list 维度）：Watch 上只有「本洞击球」，整轮列表判给 iPhone/Web——可以，但要写明，避免下轮又被当漏项翻出来。
5. **洞末负差额入口**（A7，已列入修改）。
6. **开局每步的归属面标注**（A12，已列入修改）。
7. Club Prompt 球杆列表来源（ClubProfile 真实球包）与「袋里没有的杆」的处理（跳过即可）值得一句话说明。

其余我专门查过的都在册：雨天/湿屏/手套矩阵（Track F + 16.4）、水锁 spike、Pause/Resume 生命周期、忘记结束守卫（修 Garmin 27 英里污染缺陷）、双果岭 Change Green 专项、shotgun/跳洞/9→10 会所、风与空气密度的来源+TTL、Tournament Mode 禁实打、赛后 correction 与统计重算、Watch 端最近轮次留存。

---

## F. 文档目录裁决

§17 的 docs/watch/ 树结构合理（research/product/interaction/architecture/contracts/decisions/validation/plans/archive 的权威边界划分是对的），通过。必要修改五条：

1. **decisions/ 的第一份文件必须是 owner-locked-semantics**：把本次用户十条锁定语义原文入册，标注「不可由模型联合推翻，重开需用户本人」。后续证据更正后，成绩环保留直接写为锁定决定；只为地图球童路线保留“申请重开”单。
2. **§20 增设第三队列「必须用户亲自确认」**，与「联合可直接定」「原型再定」并列——本次事故（成绩环被归入联合可决）正是缺这个队列造成的。
3. **contracts/ 给出首批清单**避免空目录：shot-ledger（candidateShot/shotStation/correction）、watch-input-event-v2、full-round-package + tee-anchors、pinSet、weather-TTL、startLie 分类的数据依赖声明（A5）。
4. **「07-10 操控宪法降级为历史输入」本身要落一条 decision**（它曾是定稿，降级是个决定，不是默认）；四份 2026-07-15 review + 两份 07-10 spec + 06-22 design 在新文档头的 Supersedes 里都要真实指回。
5. **总稿自身在 docs/watch/ 建成后也要标 superseded-by**——§17 警告了「不要膨胀成巨型总定稿」，这条纪律要对它自己生效。validation/ 直接继承 §16 矩阵 + Round 2 §13 的三级指标（硬不变量/试点观察/先基线后定门），一处维护。

---

## G. 自我攻击与证伪方法（14 条）

1. 我抽验 17 条源码断言全对，不等于其余 REPO 断言全对。证伪：给 §13 矩阵每行标源码行号，仿 `test_mobile_contracts.py` 写 grep 断言进 CI。
2. 我判 K1 为默认、K2 降对照，依据是 S70 忠实 + 平台风险；若真机上缩放冠在湿手/手套下不可用而目标轴更稳，判断反转。证伪：Map Detail 原型双臂同场测选错率/误转率/湿手成功率。
3. 单根页的工具入口可能重演「Action 键学习成本」，我维持单根页可能错。证伪：田测「新用户三洞内自行找到 Green View/计分」的比例，设失败即重启三页评估的明文条件。
4. AutoShot Beta 直做的最大暗礁是续航，我接受 Beta 可能过早。证伪：五小时高频+GPS+Workout 耗电 spike 先于任何对外承诺。
5. 我给的「≤3 秒」同样是推断。证伪：spike 实测批量到达延迟分布后收紧或放宽。
6. 洞末 +1 差额入口依赖账本与确认总分都可信；用户既漏记又乱确认时它会误导。证伪：pilot 统计该入口的接受/撤销率，低于阈值就降级为计分卡内静默提示。
7. 换洞确认式默认依赖 tee anchors 数据质量，而 CN 球场 CourseView 的 tee 坐标质量未验。证伪：抽 10 场课程包核 tee 经纬与实地偏差。
8. ~~我判成绩环冲突“必须问用户”。~~ 后续证据已证伪该前提：S70 官方明确存在成绩环，且用户此前已经 KEEP，因此无需重复询问；默认保留。
9. startLie 分类我判「面数据进包太重」可能高估。证伪：量化单洞 surface 多边形体积，若单洞 <10KB 则直接进包。
10. 推杆不逐杆是共识，但产品初心是深统计，Arccos 类竞品的果岭逐推是卖点；未来要补时 schema 不能推倒。证伪：现在就在 shot-ledger 枚举留 kind=putt 兼容位（成本≈0），不实现 UI。
11. 三页不投原型预算的判断，赌的是 Track A 不失败。证伪：把「入口不可发现→重启三页」写成明文触发器而非口头。
12. 低电改并行层可能低估了需要模态决策的场景。证伪：Track F 低电脚本跑完若出现两个以上必须决策点，恢复其队列席位。
13. PlaysLike 米/码相加虽是实锤，但典型高差下显示误差 ≤2 码，我把它列为 blocker 可能量级偏高。证伪：按现网 elevationDeltaM 分布算最大显示误差——不过它是一行修复，无论量级都该修。
14. 我判定四份文档间「无 S70 事实漂移」基于与证据包逐条比对；若证据包本身错（它依赖 2026-04 v5 手册），全链跟着错。证伪：Track H 的 S70 真机侧拍对照录制。

---

## H. 可直接写进联合文档的收口清单

### 1) 接受项（终审通过）
- 五页淘汰；三页非产品方向且不投原型预算；单一当前洞根页 IA（含浅仪表面/单交易浮层/并行状态层/持久队列）。【CONFIRMED：S70 官方结构 + 频率分析 + 四文收敛】
- 击球、成绩、当前洞三链独立；下一洞首杆 provisional 静默落盘、上一洞成绩确认优先、快速接受三合一原子操作、「继续上一洞」语义。【CONFIRMED：用户锁定 + 设计自洽】
- AutoShot v1=Beta（按 C 节修订后）；candidateShot 唯一事件语义、观测位置永不冒充击球位置、手动路径永存。【DERIVED，边界见 C】
- Club Prompt 全套语义；Tee 同点 last-wins；非 Tee 约 10 码分组保留原始观察且标注为数据实证；打厚了唯一拆分 + 三入口 + 否决自动二选一弹窗。【CONFIRMED 对齐用户锁定】
- 成绩确认双路径 + scoreRecommendation 推断对象 + fairway 无证据保持缺失；fairwayResult 可见值 HIT/LEFT/RIGHT；startLie 为每杆起始球位（枚举保留兼容位，Garmin 词表本就更宽）。【CONFIRMED】
- activePlayHole/editingHole 分离；结束不清空未同步数据、finishedPendingSync、Watch 端最近轮次留存；Finish 四出口（与 S70 官方一致）。【CONFIRMED：当前代码缺陷实锤 + 官方对照】
- Green View 独立面、pinSet 为本轮事件；Big Numbers 为持久模式；动态数据带来源与时效；PlaysLike 默认 actual 点击切换；六组正交状态；§13 全景矩阵与 §14 复用/淘汰清单（17 条源码断言抽验全对）。【CONFIRMED】

### 2) 必须修改项
① 成绩环保留写入 owner lock；地图球童路线移入「必须用户亲自确认」队列，禁止联合默决（A1/A2 与后续证据更正）；② §20 增设该第三队列（F2）；③ Map Detail 原型恢复 K2 目标停点轴对照臂并在 decisions 记录出处（A3/B）；④ §9.4 重打证据标签，0–1 秒改 ≤3 秒，Golfshot 补来源或降级（A4/C1）；⑤ 100Hz 旧设备降第二批、写死升级条件（C2）；⑥ 续航列入 Beta 发布门（C3）；⑦ §8.3 写明 startLie 分类的数据依赖与计算归属（A5）；⑧ 低电改并行警告层（A6）；⑨ 补洞末负差额「清理误报」入口并写死出现时机（A7）；⑩ 后端/消费端契约工作在 Track 里点名，双通道同步细则从 Round 2 §12 原文搬入（A8）；⑪ 「至多一个未决 episode」上限规则（A9）；⑫ Club Prompt 点按完备、表冠仅增强（A10）；⑬ 「仅一场自动选定」措辞、快速接受证据摘要一行化、开局每步标注归属面（A11/A12）；⑭ 补三条 decision：净杆 v1 不做、不记分模式下记杆照常、只记本人（E1–E3）。

### 3) 未知 / 真机门（不得冒充事实）
表冠焦点仲裁与浮层借还；根页表冠空 vs 缩放；AOD 内容/更新粒度/恢复；HKWorkout 后台 GPS 与被抢占行为；高频 API 确切机型线与批量延迟分布；五小时续航曲线；水锁与湿屏/手套矩阵；heading 质量与磁干扰；工具入口可发现性；tee anchors 数据质量；全轮包体积与传输；自动换洞几何参数；S70 真机侧拍对照。所有发布门数字先取基线（S70 对照 + 现 App 田测分布）再冻结，低频高损害错误用注入测试 + 长周期遥测证明，不用小样本「未发生」。

### 4) 下一步顺序
1. 把 2) 落回总稿；成绩环按后续官方证据恢复 KEEP，只把剩余用户确认项（球童路线、旧文档降级归档）按账本逐项讨论。
2. 建 docs/watch/ 骨架，第一份文件写 decisions/owner-locked-semantics（十条锁定语义入册）。
3. 按总稿 §18.2 顺序写前三份交互规格：scoring-and-hole-transition → shot-capture-and-recovery → club-prompt（这三份把用户锁定语义全部落地，优先级正确，维持）。
4. 并行跑会改变设计的三个 spike：高频传感 + 续航、表冠焦点仲裁、后台 Workout/GPS。
5. Track A 原型：单根页入口可发现性 + 缩放冠 vs 目标轴同场对照。
6. 之后才按能力 Track 写实施计划，禁止单线巨型计划（维持 §18.4）。

最后一点：总稿 §22「Fable 纯度审计」留白待填——本次即纯 Fable 收口审查，上述 A–H 可直接作为该节的「Fable 独立结论」输入，供 Codex 逐条接受/修改/否决后合入。
